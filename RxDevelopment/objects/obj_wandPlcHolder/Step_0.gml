/*
keyLeft = keyboard_check(vk_left) || keyboard_check(ord("A"));
keyRight = keyboard_check(vk_right) || keyboard_check(ord("D"));

if (keyLeft) {
	sprite_index = wandPlcHolderLeft;  
} else if (keyRight) {
	sprite_index = wandPlcHolder; 
} */ 